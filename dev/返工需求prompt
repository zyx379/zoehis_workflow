# Trae 返工初步分析 — 复制本文件为 dev/返工需求prompt 后填写

> Trae：只 Step 0–4，禁止改代码/git。分析结果写入本文件【分析结果】区。
> Cursor：@dev/返工需求prompt，状态=已分析 后才改代码。

【禅道号】
[203283]【漳州二院】门诊诊病界面添加预约渠道显示

【项目】
与首次交付一致

【测试反馈】
如图所示 发布后程序报错

【状态】
已分析 | 已修复

【关联记忆】（Trae 先搜 docs/memory/index.md）
- case：docs/memory/cases/2026-06-outp-appt-way-health-display.md
- 首次 commit：master `1d5ce898` / release-1.166 `b8de178d`（cherry-pick）

=====================================
【分析结果】（Trae 填写，Cursor 不改）
=====================================

【域】后端（charge-service）+ 前端 eventNo 空值守卫（master 已有）
【仓库】onelink-micro-charge-fj-common（主修复）+ onelink-web-outp-fj-common（eventNo 空值守卫已存在）
【根因】（MCP traceId=dac70e2290714a36 复核修正）charge-service `OutpApptRecordController.get` 方法标注 `@Required({""})`，`ParameterHelper.checkFields` 校验空字段名，抛出 `[]节点不能为空`（i18n KEY_NOT_NULL）。日志 requestParam 为 `{"eventNo":"D3426862"}`，**非** eventNo 为空导致。
【问题位置】`onelink-micro-charge-fj-common` — `OutpApptRecordController.java` — `/get` 接口 `@Required({""})` 配置错误
【参考实现】同 Controller 内 `getTriageQueueInfo`、`checkPatientAptRecord` 等接口使用有效字段名的 `@Required`；`/get` 为动态条件查询，Dao `getOutpApptRecord` 各字段均为可选 `<if>`
【修复建议】移除 `/get` 上错误的 `@Required({""})`；前端 `loadPatientApptWay` 保留 eventNo 空值守卫（master 已有，防御未挂号场景）
【待确认】无
【复杂度】Trivial
【下一步】修复 charge-service `OutpApptRecordController.get` 的 @Required 注解

=====================================
【修复记录】（Cursor 填写）
=====================================

- MCP 复核 traceId `dac70e2290714a36`：charge-service POST `/api/dict/reservation/outpApptRecord/get`，requestParam `{"eventNo":"D3426862"}`，异常 `[]节点不能为空`（KEY_NOT_NULL）
- 修正 Trae 分析：根因在后端 `@Required({""})`，非前端 eventNo 为空
- 改动：`OutpApptRecordController.get` 移除错误的 `@Required({""})` 注解
- 前端 `main.vue` `loadPatientApptWay` 已有 `$.zoe.isNullOrEmpty(patientInfo.eventNo)` 守卫，无需再改
