---
description: ZOEHIS AI 局部审查：异常风险、幻觉代码与 SQL 字段 MCP 核验
alwaysApply: true
---

# ZOEHIS AI 局部审查（Step 8）

改动完成后、人工审查前，对 **本次 diff** 做局部审查并输出结论。详见 `docs/workflow.md` Step 8。

## 1. 规范与数据流

- 命名、代码风格（`.cursor/rules/zoehis-*.mdc`）
- 池表/主细表/预交金/流水完整性

## 2. 运行时异常风险（必查）

针对新增/修改逻辑逐项说明是否已防护，未防护须标为 **风险** 并建议改法：

| 类型 | 检查要点 |
|------|----------|
| **空指针 NPE** | 入参、查询结果、集合元素、`Optional` 链、MyBatis 返回 null、前端 `res.data` 未判空 |
| **SQL 异常** | 字段名/表名是否存在；`#{}` 类型与库字段；动态 SQL 空 `IN ()`；达梦/Oracle 方言；主键冲突、违反非空 |
| **索引越界** | 数组/List 下标、`substring`/`charAt`、前端表格行号、`split` 后取 `[0]` |
| **类型/转换** | 数字格式、日期解析、`BigDecimal` 比较、前后端字段类型不一致 |
| **并发/事务** | 多表写是否同事务；池表删增是否原子 |

## 3. 幻觉代码（必查）

禁止未经验证就写入的「猜测性」实现：

- 不存在的 **类、方法、接口路径、配置项**
- 不存在的 **表名、SQL 列名、resultMap 属性**
- 编造的业务状态码、字典项、枚举值

**SQL/Dao.xml 列名、表名存疑时：** 必须调用 MCP `user-zoe-his-mcp` → `get_table_schema(tableNamePattern)`，以返回字段为准；不符则修正后再进入 Step 9。

**禁止** 仅凭记忆或相似表结构写列名；schema 结果须在审查结论中引用（表名 + 关键字段列表摘要）。

## 4. 审查输出格式

```markdown
## AI 局部审查

### 通过项
- ...

### 异常风险
- [ ] 无 / 或列出项 + 建议

### 幻觉核验
- [ ] 无幻觉 / 已用 get_table_schema 核对：<表名> → 字段 ...

### 结论
通过 / 需修改（回到 Step 6）
```

存在 **未修复的高风险** 或 **未核验的存疑 SQL 字段** → 结论为 **需修改**，不得进入 Step 9。
